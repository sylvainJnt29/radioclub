{% extends 'base.html.twig' %}

{% block title %}Edition Article{% endblock %}

{% block myTitle %}Edition de l'article{% endblock %}

{% block body %}
<div class="container">
     <div class="text-center">
         <img src="{{asset('images/' ~ article.image)}}" alt=""  style="height: 200px; width: 200px;">
     </div>

    <!-- La mep ne sert à rien :) -->
    <div class="boutons_crud"> 
        <div>
            {{ include('article/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
        
        <div>
            {{ include('article/_delete_form.html.twig') }}
        </div>
        
        <div>
            <button class="btn btn-info"><a href="{{ path('articles_admin') }}">Retour à la liste</a></button>
        </div>
    </div>
</div>  
{% endblock %}
