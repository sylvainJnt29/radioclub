{% extends 'base.html.twig' %}

{% block title %}Edition Photo{% endblock %}

{% block myTitle %}Edition de la photo{% endblock %}

{% block body %}
<div class="container">
     <div class="text-center">
         <img src="{{asset('images/' ~ photo.image)}}" alt=""  style="height: 200px; width: 200px;">
     </div>
    <div>
        {{ include('photo/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div>
        {{ include('photo/_delete_form.html.twig') }}
    </div>
    <div>
        <button class="btn btn-info"><a href="{{ path('photos_admin') }}">Retour à la liste</a></button>
    </div>
</div>
{% endblock %}
