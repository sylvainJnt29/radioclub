{% extends 'base.html.twig' %}

{% block title %}Détails de la photo{% endblock %}

{% block myTitle %}Détails de la photo{% endblock %}

{% block body %}


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ photo.image }}</td>
            </tr>
            <tr>
                <th>Visuel</th>
                <td><div class="image_visu"><img src="{{asset('images/' ~ photo.image)}}" alt=""  style="height: 200px; width: 200px;"></div></td>
            </tr>
            <!-- <tr>
                <th>Updated_at</th>
                <td>{{ photo.updatedAt ? photo.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr> -->
        </tbody>
    </table>

    <button class="btn btn-info"><a href="{{ path('photos_admin') }}">Retour à la liste</a></button>
    <br>
    <button class="btn btn-success"><a href="{{ path('photo_edit', {'id': photo.id}) }}">Editer</a></button>

    {{ include('photo/_delete_form.html.twig') }}
{% endblock %}
